---
layout: base.njk
title: Field Notes
---

<div class="blog-grid blog-grid--field-notes">
  <div class="blog-grid__pin" aria-hidden="true">
    <img src="/images/pin.png" alt="" width="72" height="72" decoding="async"/>
  </div>
  {% for post in collections.posts %}
    {% set tilt = loop.index0 | boardTiltStyle %}
    {% set img = post.data.image | default("/images/logo.png") | polaroidSrc %}
    <a class="blog-card{% if post.data.pinned %} blog-card--pinned{% endif %}" href="{{ post.url }}" style="{{ tilt }}">
      {% if post.data.indexPolaroid %}
        <div class="polaroid">
          <div class="tape tape-left"></div>
          <div class="tape tape-right"></div>
          <div class="polaroid-image">
            <img src="{{ img }}" alt="{{ post.data.title }}"/>
          </div>
          <div class="polaroid-caption font-handwriting">
            <h3>{{ post.data.title }}</h3>
          </div>
        </div>
      {% else %}
        <div class="note-card">
          <div class="tape tape-left"></div>
          <div class="tape tape-right"></div>
          <div class="note-header">
            <img
              class="note-blood-drop {{ '' | bloodDropPlacement }}"
              src="/images/blood_drop.png"
              alt=""
              width="48"
              height="48"
              decoding="async"
              aria-hidden="true"/>
            <div class="note-header__copy">
              <span class="case-label">
                {{ post.data.year }}{{ post.data.case_type }}-{{ post.data.case_number | default(0) | padSuffix }}.{{ post.data.session | default(0) | padSuffix }}
              </span>
              <h2 class="note-title font-handwriting">{{ post.data.title }}</h2>
            </div>
            {% if post.data.image %}
              <img src="{{ post.data.image | polaroidSrc }}" alt="" class="note-thumbnail" width="80" height="80"/>
            {% endif %}
          </div>
          {% set manualExcerpt = post.data.excerpt | default(post.data.description) %}
          <p class="note-excerpt font-typewriter">
            {% if manualExcerpt %}
              {{ manualExcerpt }}
            {% else %}
              {{ post.templateContent | plainExcerpt }}
            {% endif %}
          </p>
          <div class="note-footer">
            <span class="note-date">{{ post.date | longDate }}</span>
          </div>
          <div class="evidence-stamp">
            <span class="category-stamp__text">{{ post.data.category | default("Field notes") }}</span>
          </div>
        </div>
      {% endif %}
      {% if post.data.pinned %}
        <span class="blog-card__pin" aria-hidden="true">
          <img src="/images/pin.png" alt="" width="40" height="40" decoding="async"/>
        </span>
      {% endif %}
    </a>
  {% else %}
    <p class="font-typewriter blog-grid__empty">
      No field notes filed yet.
    </p>
  {% endfor %}
</div>